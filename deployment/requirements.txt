# DistriNews Deployment Dependencies
# ==================================
# Minimal dependencies for inference only (no training)

# PyTorch (CPU version for deployment - smaller image)
torch>=2.1.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Transformers for DistilBERT
transformers>=4.36.0

# FastAPI for the API server
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
